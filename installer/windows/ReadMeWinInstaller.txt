To produce the Windows installer:

Make sure the program has been compiled.

In a command prompt, make sure have the path set to the correct version of Java and WiX.  (See setPath.bat)

Set the java_path to where you have your java JDK in BuildWin.bat

Invoke BuildWin.bat versionNumber

Note:

You will need to change the following as appropriate:

In WinInstaller.bat, if needed, set the copyright date.
